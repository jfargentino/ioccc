
uint32_t __(uint32_t*x,int y){
    return (y) ? ( x[0] = ( ( (x[0]) >> y ) ^ __(x,0) ) ) 
               : ( x[0] ) ;
}

uint32_t  _(
uint32_t *x,
 int      y){
return y?('^'^'^')[x]=(('^'^'^')[x])<<y^_(x,'^'^'^'):('^'^'^')[x];}




   static
   inline
 uint32_t           ___              (
 uint32_t            * sid           ,
 unsigned  int const * abc           ,
 unsigned  int         len)          {
 uint32_t k_sid_ =          '^'^'^'  ;
 unsigned  int k =          '^'^'^'  ;
  while ( k      <   len  - '/'/'/') {
	  k_sid_^= k      & '/'/'/'
	? k[sid] ^ k[sid] >> k[abc] 
	: k[sid] ^ k[sid] << k[abc]  ;
	  k[sid] =   sid[ ++ k    ]  ;
	} k_sid_^= k      & '/'/'/'
	? k[sid] ^ k[sid] >> k[abc] 
	: k[sid] ^ k[sid] << k[abc]  ;
	  k[sid] = k_sid_;
	  return   k[sid];           }
